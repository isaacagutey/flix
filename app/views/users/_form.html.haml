= bootstrap_form_for(User.new, label_col: "col-sm-3", control_col: "col-sm-3",inline_errors: true) do |f| 
  = f.email_field :email, required: :true
  = f.password_field :password, required: :true
  = f.text_field :full_name, required: :true
  = f.form_group do 
    = f.submit "Sign Up", class:"btn btn-default signup"

%p
  %a Already a member? Sign in.
